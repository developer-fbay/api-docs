info:
  name: Gets authentication token
  type: http
  seq: 1
  tags:
    - authorisation

http:
  method: POST
  url: "{{baseUrl}}/get_token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
